package com.lucassdalmeida.writing.domain.model.thread

import com.lucassdalmeida.writing.domain.model.volume.VolumeId

class VolumeThread(id: NarrativeThreadId, val volumeId: VolumeId) : NarrativeThread(id)
